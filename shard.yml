name: monocypher
version: 2.0.5

authors:
  - Konovod <konovod@gmail.com>

crystal: 0.26.0

scripts:
  postinstall: "mkdir ./.build; cc ./ext/monocypher.c -c -o ./.build/monocypher.o -O3 -march=native -std=gnu99; cc ./ext/sha512.c -c -o ./.build/sha512.o -O3 -march=native -std=gnu99; ar rcs ./.build/libmonocypher.a ./.build/monocypher.o ./.build/sha512.o"

license: MIT
